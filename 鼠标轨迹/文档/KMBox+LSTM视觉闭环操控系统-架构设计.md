# KMBox + LSTM 视觉闭环操控系统 — 架构设计与可行性分析

## 背景

目标：构建一套**视觉驱动的闭环自动操作系统**。PC A 运行游戏，视频由 HDMI 环出到 PC B 的采集卡；PC B 运行 CV 视觉分析 + 决策逻辑 + LSTM 轨迹生成，通过 KMBox 硬件向 PC A 注入键鼠操作。全程软件层面 PC A 不跑任何第三方程序。

---

## 一、物理拓扑

```
┌──────────────────────────────────────────────────────────────────┐
│                         PC A (被控机/游戏机)                        │
│                                                                  │
│  运行游戏 ──▶ GPU渲染 ──▶ HDMI OUT ──┐                            │
│                                      │                            │
│  ◀── KMBox USB (识别为鼠标+键盘)       │                            │
└──────────────────────────────────────┼────────────────────────────┘
                                       │ HDMI 视频流
                                       ▼
┌──────────────────────────────────────┼────────────────────────────┐
│                         PC B (控制机/AI主机)                        │
│                                      │                            │
│  采集卡 HDMI IN ◀────────────────────┘                            │
│       │                                                           │
│       ▼                                                           │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌────────────┐  │
│  │ 屏幕截图  │───▶│ CV 检测   │───▶│ 决策引擎  │───▶│ LSTM 轨迹  │  │
│  │ (60fps)  │    │ (YOLO等) │    │ (策略)   │    │ 生成 (MDN) │  │
│  └──────────┘    └──────────┘    └──────────┘    └──────┬─────┘  │
│                                                        │         │
│                                                        ▼         │
│                              KMBox ◀── USB/串口 ◀── 轨迹点序列     │
│                                │                                   │
└────────────────────────────────┼───────────────────────────────────┘
                                 │ USB (被识别为键鼠HID)
                                 ▼
                    PC A 收到硬件级键鼠输入 ◀── 闭环
```

**信号流向总结**：
```
游戏画面: PC A GPU → HDMI → 采集卡 → PC B 内存 (frame buffer)
控制信号: PC B 推理 → 串口/USB → KMBox → USB HID → PC A
```

---

## 二、KMBox 型号选择

用户场景：**双机分离 + 视频回传**。PC B 做所有重计算，KMBox 只做执行器。

| 型号 | 双机支持 | 通信方式 | Python脚本 | 适合? |
|------|---------|---------|-----------|-------|
| **A / A+** | A+有 | HID | 无/A有宏 | ❌ HID协议能力弱，不适合逐点轨迹传输 |
| **B** | 需扩展板 | 串口 | 4MB | ⚠️ 可以但双机需额外硬件 |
| **B Pro (B+)** ★ | **原生双机** | 串口 | 4MB | ✅ **最佳选择**，USB口分离设计天然契合 |
| **Net** | 有 | 以太网 | 无 | ⚠️ 网络通信延迟更低但无Python板载 |
| **NVideo** | 有 | 以太网 | 无 | ❌ 内置采集卡重复了，用户已有HDMI采集方案 |
| **NB** | 有 | 网络+串口 | 50MB | ⚠️ 性能过冗余，除非需要8000Hz |
| **AI** | 有 | WiFi | SD卡 | ❌ 板载NPU/YOLO在本架构中用不上 |

### ★ 推荐：KMBox B Pro (B+)

理由：
- **USB口天然分离**：USB1→PC A，USB2→PC B，USB3/4→实体键鼠（需要时可人工接管）
- **串口通信**：PC B 通过 COM 口发送轨迹指令，协议成熟，Python `pyserial` 直接对接
- **双核 240MHz**：足够处理逐点轨迹缓冲和时序执行
- **1000Hz 回报率**：每点 1ms 间隔，轨迹点间隔通常 8-16ms（125Hz），绰绰有余
- **性价比**：不需要为用不上的 NPU/8K/视频采集 付溢价

---

## 三、全链路数据流与延迟分析

### 3.1 处理流水线（逐帧）

```
Frame N (PC A 渲染)
    │
    ├──[1] HDMI 传输延迟 ......................... <1ms (数字信号)
    ├──[2] 采集卡捕获 + DMA 到内存 ................ 5-15ms (取决于采集卡)
    ├──[3] CV 推理 (YOLO/OCR/模板匹配) ............ 5-30ms (取决于模型和GPU)
    ├──[4] 决策逻辑 (状态机/规则/RL) ............... <1ms
    ├──[5] LSTM-MDN 轨迹生成 (20-100步) ............ <5ms (GPU) / <20ms (CPU ONNX)
    ├──[6] 串口发送轨迹序列到 KMBox ................ 1-5ms (115200bps, 50点≈100字节)
    └──[7] KMBox 逐点执行鼠标移动 .................. 取决于轨迹时长 (设计目标: 100-500ms)
         │
         ▼
Frame N+1 (PC A 渲染，此时鼠标已移动到新位置)
```

### 3.2 延迟瓶颈识别

| 环节 | 延迟 | 是否瓶颈 | 优化手段 |
|------|------|---------|---------|
| HDMI 传输 | <1ms | ❌ | — |
| 采集卡捕获 | 5-15ms | ⚠️ | 选低延迟采集卡 (如 PCIe 接口的 Elgato 4K60 Pro / 圆刚 GC573) |
| CV 推理 | 5-30ms | ⚠️ | 轻量模型 (YOLOv8-nano / ONNX), GPU 推理, ROI 裁剪 |
| 决策逻辑 | <1ms | ❌ | — |
| LSTM 生成 | <5ms | ❌ | GPU 批量推理或 ONNX CPU |
| 串口发送 | 1-5ms | ❌ | — |
| KMBox 执行 | 100-500ms | ⚠️ | 这是设计变量——轨迹时长需要在"自然感"和"响应速度"间平衡 |

**关键认知**：KMBox 轨迹执行时间（100-500ms）占整个链路延迟的主导。这意味着：
- 对于**回合制/RPG/策略游戏**：500ms 延迟完全可接受
- 对于**MOBA/MMO**：200ms 左右可接受（人类反应时间 ~200-300ms）
- 对于**FPS/格斗**：需要极致优化全链路到 100ms 以内，有一定挑战

---

## 四、软件架构（PC B 端）

### 4.1 进程架构

```
┌────────────────────────────────────────────────┐
│                   Main Loop                     │
│           (主循环, 异步事件驱动)                  │
├────────────────────────────────────────────────┤
│                                                 │
│  Thread 1: Frame Grabber (采集卡读取)            │
│  ┌──────────────────────────────────────┐       │
│  │ 采集卡 → numpy.ndarray (HWC, BGR)      │       │
│  │ framerate: 60fps                      │       │
│  └──────────────┬───────────────────────┘       │
│                 │ frame_ready event              │
│                 ▼                                │
│  Thread 2: Vision Pipeline (CV推理)              │
│  ┌──────────────────────────────────────┐       │
│  │ YOLO 检测 → OCR 文字 → 模板匹配       │       │
│  │ 输出: GameState (entities, UI, map)   │       │
│  └──────────────┬───────────────────────┘       │
│                 │ game_state                     │
│                 ▼                                │
│  Thread 3: Decision Engine (决策)                │
│  ┌──────────────────────────────────────┐       │
│  │ 状态机/行为树/RL → Action             │       │
│  │ Action: {type: "click", target: (x,y),│       │
│  │          speed: "fast"}               │       │
│  └──────────────┬───────────────────────┘       │
│                 │ action                         │
│                 ▼                                │
│  Thread 4: Trajectory Generator (轨迹)           │
│  ┌──────────────────────────────────────┐       │
│  │ LSTM-MDN 模型推理                      │       │
│  │ 输入: (current_pos, target_pos, speed) │       │
│  │ 输出: [(x1,y1,dt1), (x2,y2,dt2), ...] │       │
│  └──────────────┬───────────────────────┘       │
│                 │ trajectory points              │
│                 ▼                                │
│  Thread 5: KMBox Executor (硬件执行)              │
│  ┌──────────────────────────────────────┐       │
│  │ 串口逐点发送, 精确时序控制             │       │
│  │ 执行完成后 fire action_done event     │       │
│  └──────────────────────────────────────┘       │
│                                                 │
└────────────────────────────────────────────────┘
```

### 4.2 关键设计决策

#### 流水线 vs 帧同步？

**建议：异步流水线模式。** 每帧不是"等上一帧执行完"，而是：
- Frame grabber 以固定 60fps 抓帧
- 如果上一帧的 KMBox 轨迹还在执行中，**丢弃**当前帧（或用新帧更新目标位置做动态轨迹修正）
- 避免积压导致延迟雪崩

#### 动态轨迹修正（高价值特性）

如果 KMBox 正在执行 A→B 的轨迹，但 CV 检测到目标移动了（B→B'），有两种策略：
1. **中断重算**：发送停止指令，从当前位置重新生成到 B' 的轨迹（可能有突兀的急转弯）
2. **在线修正**：修改剩余轨迹点的目标方向，平滑过渡（实现复杂但更自然）——适合 Phase 2+

---

## 五、CV 视觉分析模块设计

### 5.1 典型游戏场景的检测需求

| 检测目标 | 方法 | 模型/工具 |
|---------|------|----------|
| 游戏内物体/角色 | 目标检测 | YOLOv8-nano (ONNX) |
| UI 按钮/文字 | OCR + 模板匹配 | PaddleOCR / Tesseract + OpenCV matchTemplate |
| 地图/小地图位置 | 模板匹配 | OpenCV, 已知地图图标 |
| 血条/状态条 | 颜色阈值 + 形态学 | OpenCV |
| 屏幕变化检测 | 帧差法 | OpenCV absdiff |

### 5.2 CV 推理优化

```python
# 关键优化技巧
1. ROI 裁剪: 只推理感兴趣区域，如小地图区域、UI 栏区域
2. 跳帧推理: 不需要每帧跑 CV，可以每 2-3 帧跑一次
3. FP16/INT8 量化: YOLO ONNX 模型 int8 量化后推理 <5ms
4. 异步推理: CV 推理放独立线程，帧抓取不等待
```

### 5.3 检测方法的选择逻辑：固定 vs 动态

CV 检测目标可以按"是否固定不变"分为两类，分别走不同的技术路线：

```
计算机视觉 (CV)

  ├── 固定不变的目标 ──→ OpenCV 模板匹配（零训练，毫秒级）
  │     鼠标光标、背包图标、确定/取消按钮、血条框架
  │     方法：cv2.matchTemplate(画面, 模板图)
  │     原理：逐像素滑动对比相似度——目标永远长一个样
  │
  └── 会变的目标 ──→ YOLO（需要标注训练）
        玩家角色、NPC、怪物、可移动物体
        方法：标几千张图 → 训练 → 推理
        原理：学会"有头有身子像个人"的语义概念，不是记像素
```

#### 为什么光标永远适合模板匹配

游戏光标是**系统级的叠加层**，渲染在所有游戏画面之上。没有任何 NPC、玩家、特效可以盖住它。它永远完整可见，永远长同一个样。模板匹配对它极其可靠，`cv2.matchTemplate` 一行代码，毫秒级返回位置。

#### 什么时候 YOLO 才出场

只有目标**每次出现长得不一样、或者可能被遮挡**时才需要：
- 在人群里找一个特定 NPC（被玩家遮挡、穿着不同皮肤）
- 识别一个正在移动的怪物（姿态变化）
- 检测屏幕上的玩家角色（不同角度不同外观）

这些不是"匹配一个固定图样"能解决的，必须用学习到的语义概念来识别。

#### 反馈修正中的 CV 角色

CV 在反馈修正系统中承担两个职责，全都是模板匹配，不需要 YOLO：

```
反馈修正的时间线：

  0ms  ─────────────────── 800ms ── 820ms ── 850ms
  │                         │       │        │
  │  LSTM 200点轨迹播放      │       │        │
  └────────────────────────→│       │        │
                             │       │        │
                           CV 检测  偏差 15px │
                             │       │ 补发   │
                             └───────┴───────→│ 完成

Step 1: 模板匹配找UI元素位置（如"确定"按钮 (1000, 600)）  ← OpenCV, ~2ms
Step 2: 模板匹配找当前光标位置 (815, 420)               ← OpenCV, ~2ms
Step 3: LSTM 生成轨迹 (815,420) → (1000,600)            ← 模型推理, ~5ms
Step 4: KMBox 播放轨迹                                   ← 取决于轨迹时长
Step 5: 模板匹配确认光标是否到达 (1000,600)              ← OpenCV, ~2ms
        偏差 < 阈值 → 跳过修正；偏差 > 阈值 → 补发微调
```

**LSTM 做 95% 的活（大轨迹），CV 模板匹配只做最后 5% 的纠偏（确认落点 + 按需微调）。** 不是每帧都闭环，只兜底一两次，避免积压延迟雪崩。

---

## 六、硬件采购清单

| 设备 | 推荐型号 | 预算参考 | 用途 |
|------|---------|---------|------|
| **KMBox** | B Pro (B+) | ¥200-400 | 键鼠硬件模拟 |
| **采集卡** | 圆刚 GC573 (PCIe) 或 Elgato 4K60 Pro | ¥800-1500 | 低延迟 HDMI 采集 |
| **HDMI 线** | HDMI 2.0 短线 (≤2m) | ¥30-50 | PC A→采集卡 |
| **USB 线** | USB-A to USB-A 优质线 | ¥20 | KMBox→PC A |
| **串口线** | Micro USB 数据线 | ¥20 | KMBox→PC B |

> **关于采集卡**：如果用 USB 采集卡会有额外 30-50ms 延迟，强烈建议用 **PCIe 接口**的内置采集卡。如果 PC B 是笔记本无法插 PCIe 卡，则退而求其次选 Elgato HD60 X (USB, 延迟约 30ms)，但要做好延迟管理的心理准备。

---

## 七、核心挑战与缓解策略

### 挑战 1：全链路延迟

```
理想总延迟 = 采集(5ms) + CV(10ms) + LSTM(2ms) + 轨迹执行(150ms) = ~167ms
可接受范围: <200ms (RPG/MMO), <100ms (MOBA/动作游戏)
```

**缓解**：
- PCIe 采集卡 (≤5ms) 而非 USB (≥30ms)
- YOLO 轻量模型 + FP16 + ROI 裁剪
- 短距离移动用短轨迹 (<50pt)，长距离才用全轨迹

### 挑战 2：CV 检测的鲁棒性

同一物体在不同光照/特效/皮肤下可能检测失败。

**缓解**：
- 多模型级联：先跑快速的模板匹配，置信度低时才跑 YOLO
- 时序平滑：连续 N 帧检测结果取平均/中值，过滤单帧误检
- 采集 1000+ 张游戏截图做数据增强后微调 YOLO

### 挑战 3：轨迹的"上下文感知"

LSTM 只知起点终点，不知屏幕上的障碍物（UI 面板、地形）。

**缓解**：
- 决策层判断是否需要"绕行"，如果需要，生成中间途经点 (waypoint)，分段生成轨迹
- 轨迹的后处理过滤器：检测生成轨迹是否穿过"禁区"，如有则拒绝+重新生成

### 挑战 4：反作弊检测

这个架构比单机软件方案隐蔽得多，但仍有理论风险：

| 检测向量 | 风险等级 | 缓解 |
|---------|---------|------|
| USB HID 描述符指纹 | 低 | KMBox 使用通用 HID 描述符 |
| 鼠标轨迹统计特征 | **高** | LSTM-MDN 多样性 + 对抗训练 |
| 行为一致性 (总是相同延迟) | 中 | 轨迹时序加随机抖动 |
| 视频采集卡检测 | 极低 | 采集卡是纯接收设备，无信号输出 |

### 挑战 5：游戏坐标系偏移与轨迹修正策略

#### 问题根源

桌面模式下面布坐标与物理鼠标是 1:1 线性映射，开环精度足够。但游戏模式下，KMBox 发出的物理 delta 经过游戏引擎处理（加速度曲线、钳制、平滑插值）后，游戏内光标位移与物理输入不线性对应。此外，帧率波动（30fps vs 60fps）下相同物理输入产出不同光标位移。

虽然 LSTM 训练数据本身就是物理轨迹，模型学到的映射已隐式包含游戏变换的补偿，但残余偏差依然存在（通常几十像素），需要反馈修正兜底。

#### 三种修正策略对比

| 策略 | 做法 | 问题 |
|------|------|------|
| **A. 纯开环** | LSTM 生成 → KMBox 播放 → 结束 | 残余偏差累积，落点不可控 |
| **B. 逐帧闭环** | 每发若干 delta → CV 查位置 → 调整 → 继续 | CV 检测 + 采集卡延迟 > 16ms，跟不上 60fps，延迟雪崩；且中途打断破坏 LSTM 轨迹的连贯丝滑度 |
| **C. 开环 + 完成后微调** ★ | LSTM 整条轨迹走完 → CV 查一次落点 → 偏差超阈值则补发小步修正 | ✅ 不打断轨迹连贯性，修正只兜底一两次 |

#### 方案 C 的完整时间线

```
0ms  ─────────────────── 800ms ── 820ms ── 850ms ── 870ms
│                         │       │        │        │
│  LSTM 200点轨迹播放      │       │        │        │
│  丝滑弧线、自然变速       │       │        │        │
└────────────────────────→│       │        │        │
                           │       │        │        │
                         CV 检测   偏差 18px │        │
                         (~2ms)     > 阈值   │        │
                           │       补发小修正  │        │
                           └───────┴───────→│  CV 再确认
                                             偏差 3px < 阈值
                                             → 结束
```

全程只在轨迹执行完成后介入，不中途打断，不破坏运动连贯性。

#### 长距离 vs 短距离：用什么工具

```
距离       用什么                     人类对应动作
──────────────────────────────────────────────────
> 100px    LSTM 生成完整轨迹           手臂带动手腕画弧线
           弧线、变速、终点自然减速
< 30px    直接算 delta 发给 KMBox     手指微调，一瞬间
           不需要 LSTM
```

短距离微调不用 LSTM 的原因：
- 人类微调本身就没有"轨迹"可言——手指一抖就过去了，没有弧线、没有减速过程
- LSTM 在极短距离下输出趋同（起点终点太近，归一化后条件向量几乎一样）
- 直接发 delta 在十几像素尺度上与人类微调行为不可区分

#### 桌面模式 vs 游戏模式

| | 桌面模式（已实现） | 游戏模式（未来） |
|---|---|---|
| 坐标系映射 | 1:1 线性 | 非线性（游戏引擎变换） |
| 修正需求 | 不需要，开环精度高 | 需要完成后微调 |
| 数据来源 | pynput 录制 | KMBox + 采集卡录制 |
| 当前状态 | ✅ 已打通 | ⬜ 待实施 |

---

## 八、实施路线图

### Phase 1：链路打通（1-2 周）
- [ ] 采购 KMBox B Pro + PCIe 采集卡
- [ ] PC B 能通过采集卡看到 PC A 画面
- [ ] KMBox 串口通信验证：PC B → KMBox → PC A 鼠标能动
- [ ] 最简单的固定轨迹发送验证（不涉及 LSTM）

### Phase 2：CV 感知（2-3 周）
- [ ] 采集卡帧抓取线程
- [ ] 针对目标游戏的 CV 检测模型选型与微调
- [ ] GameState 数据结构定义
- [ ] 检测结果可视化叠加层（调试用）

### Phase 3：LSTM 轨迹生成（2-3 周）
- [ ] 鼠标轨迹数据采集工具
- [ ] 数据预处理管道
- [ ] LSTM-MDN 模型训练
- [ ] ONNX 导出 + 推理测试

### Phase 4：决策引擎 + 全链路集成（2 周）
- [ ] 简单状态机 / 规则引擎
- [ ] CV → 决策 → LSTM → KMBox 全链路联调
- [ ] 延迟 benchmark + 优化

### Phase 5：对抗性评估（持续）
- [ ] 轨迹自然度量化评估（DTW / 速度分布）
- [ ] 长期运行稳定性测试

---

## 九、技术栈

| 层 | 选型 |
|----|------|
| 帧抓取 | OpenCV `VideoCapture` 或 DirectShow SDK |
| CV 检测 | YOLOv8-nano ONNX + PaddleOCR + OpenCV |
| 决策引擎 | 自研 Python 状态机 |
| 轨迹生成 | PyTorch LSTM + MDN → ONNX Runtime |
| KMBox 通信 | `pyserial` (COM口) |
| 进程通信 | Python `threading` + `queue.Queue` |
| 可视化调试 | OpenCV `imshow` / Flask 网页面板 |

---

## 十、为什么不选择其他方案

| 替代方案 | 劣势 |
|---------|------|
| 单机软件 (SendInput/autogui) | 被反作弊检测的风险极高；注入到游戏进程空间会触发完整性检查 |
| DMA 硬件 | 需要定制固件 + FPGA 开发，成本 ¥2000+，技术门槛极高 |
| Arduino Leonardo 模拟 | 仅支持简单宏，无 Python 扩展能力，不能逐点轨迹 |
| 直接远程桌面/VNC | 画面延迟大，键鼠输入特征明显不同 |
